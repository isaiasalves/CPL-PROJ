programa
declare	int a, int b, String x .
	escreva("Meu primeiro prog em Comp").
	leia( a ).
	leia( b ).
	se (a == b) entao {
		escreva("O resultado da soma vale").
	} senao {
		escreva("O resultado da soma NAO vale").
	}

	enquanto (a<b){
		a := a + b.
	}
	
	faca{
		b := a + b.
	}enquanto(b>a).
fimprog
